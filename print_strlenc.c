#include "main.h"

/**
 * _strlenc - outputs the length of a constant character string
 * @str: char (pointer)
 * Return: i.
 */
int _strlenc(const char *str)
{
	int i,

	    for (i = 0; str[i] !=0; i++);
	return (i)
}
